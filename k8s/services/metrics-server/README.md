# Initium: Kubernetes Services: Metrics Server
Metrics server deployment.

Note: This tweaks the standard deployment to set (--kubelet-insecure-tls) which is NOT 
      recommended for production deployment